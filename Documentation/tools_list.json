{
  "result": {
    "tools": [
      {
        "name": "mssql_test_connection",
        "description": "Test a database connection.",
        "inputSchema": {
          "title": "mssql_test_connection",
          "description": "Test a database connection.",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": { "connectionString": { "type": "string" } },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_rotate_key",
        "description": "Rotate encryption key for connection strings",
        "inputSchema": {
          "title": "mssql_rotate_key",
          "description": "Rotate encryption key for connection strings",
          "type": "object",
          "properties": { "newKey": { "type": "string" } },
          "required": ["newKey"]
        }
      },
      {
        "name": "mssql_get_table_metadata",
        "description": "Gets detailed metadata about the database tables, columns, primary keys and foreign keys.",
        "inputSchema": {
          "title": "mssql_get_table_metadata",
          "description": "Gets detailed metadata about the database tables, columns, primary keys and foreign keys.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            },
            "schema": { "type": "string", "default": null }
          }
        }
      },
      {
        "name": "mssql_get_agent_job_details",
        "description": "Gets detailed information (steps, schedules, history) for a specific SQL Server Agent job.",
        "inputSchema": {
          "title": "mssql_get_agent_job_details",
          "description": "Gets detailed information (steps, schedules, history) for a specific SQL Server Agent job.",
          "type": "object",
          "properties": {
            "jobName": { "type": "string" },
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          },
          "required": ["jobName"]
        }
      },
      {
        "name": "mssql_list_connections",
        "description": "List all available database connections.",
        "inputSchema": {
          "title": "mssql_list_connections",
          "description": "List all available database connections.",
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "mssql_get_key_usage_logs",
        "description": "Get recent usage logs for a specific API key (admin only)",
        "inputSchema": {
          "title": "mssql_get_key_usage_logs",
          "description": "Get recent usage logs for a specific API key (admin only)",
          "type": "object",
          "properties": {
            "apiKeyId": { "type": "string" },
            "limit": { "type": "integer", "default": 100 }
          },
          "required": ["apiKeyId"]
        }
      },
      {
        "name": "mssql_initialize_connection",
        "description": "Initialize the SQL Server connection.",
        "inputSchema": {
          "title": "mssql_initialize_connection",
          "description": "Initialize the SQL Server connection.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          }
        }
      },
      {
        "name": "mssql_get_user_usage_logs",
        "description": "Get recent usage logs for a user (admin only)",
        "inputSchema": {
          "title": "mssql_get_user_usage_logs",
          "description": "Get recent usage logs for a user (admin only)",
          "type": "object",
          "properties": {
            "userId": { "type": "string" },
            "limit": { "type": "integer", "default": 100 }
          },
          "required": ["userId"]
        }
      },
      {
        "name": "mssql_remove_connection",
        "description": "Remove a database connection.",
        "inputSchema": {
          "title": "mssql_remove_connection",
          "description": "Remove a database connection.",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": { "name": { "type": "string" } },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_get_database_objects_by_type",
        "description": "Gets detailed metadata about specific database object types.",
        "inputSchema": {
          "title": "mssql_get_database_objects_by_type",
          "description": "Gets detailed metadata about specific database object types.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            },
            "schema": { "type": "string", "default": null },
            "objectType": { "type": "string", "default": "ALL" }
          }
        }
      },
      {
        "name": "mssql_revoke_key",
        "description": "Revoke an API key",
        "inputSchema": {
          "title": "mssql_revoke_key",
          "description": "Revoke an API key",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": { "id": { "type": "string" } },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_list_user_keys",
        "description": "List API keys for a user",
        "inputSchema": {
          "title": "mssql_list_user_keys",
          "description": "List API keys for a user",
          "type": "object",
          "properties": { "userId": { "type": "string" } },
          "required": ["userId"]
        }
      },
      {
        "name": "mssql_generate_key",
        "description": "Generate a secure random key for connection string encryption",
        "inputSchema": {
          "title": "mssql_generate_key",
          "description": "Generate a secure random key for connection string encryption",
          "type": "object",
          "properties": { "length": { "type": "integer", "default": 32 } }
        }
      },
      {
        "name": "mssql_create_key",
        "description": "Create a new API key for a user",
        "inputSchema": {
          "title": "mssql_create_key",
          "description": "Create a new API key for a user",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "userId": { "type": "string" },
                "keyType": { "type": "string" },
                "expirationDate": { "type": ["string", "null"] },
                "allowedConnectionNames": {
                  "type": "array",
                  "items": { "type": "string" }
                }
              },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_get_database_objects_metadata",
        "description": "Gets detailed metadata about database objects including tables and views.",
        "inputSchema": {
          "title": "mssql_get_database_objects_metadata",
          "description": "Gets detailed metadata about database objects including tables and views.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            },
            "schema": { "type": "string", "default": null },
            "includeViews": { "type": "boolean", "default": true }
          }
        }
      },
      {
        "name": "mssql_add_connection",
        "description": "Add a new database connection.",
        "inputSchema": {
          "title": "mssql_add_connection",
          "description": "Add a new database connection.",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "connectionString": { "type": "string" },
                "description": { "type": ["string", "null"] }
              },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_get_ssis_catalog_info",
        "description": "Gets SSIS catalog information including Project Deployment and Package Deployment models.",
        "inputSchema": {
          "title": "mssql_get_ssis_catalog_info",
          "description": "Gets SSIS catalog information including Project Deployment and Package Deployment models.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          }
        }
      },
      {
        "name": "mssql_update_connection",
        "description": "Update an existing database connection.",
        "inputSchema": {
          "title": "mssql_update_connection",
          "description": "Update an existing database connection.",
          "type": "object",
          "properties": {
            "request": {
              "type": "object",
              "properties": {
                "name": { "type": "string" },
                "connectionString": { "type": "string" },
                "description": { "type": ["string", "null"] }
              },
              "additionalProperties": false
            }
          },
          "required": ["request"]
        }
      },
      {
        "name": "mssql_migrate_connections",
        "description": "Migrate unencrypted connection strings to encrypted format",
        "inputSchema": {
          "title": "mssql_migrate_connections",
          "description": "Migrate unencrypted connection strings to encrypted format",
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "mssql_get_azure_devops_info",
        "description": "Gets Azure DevOps information including projects, repositories, builds, and work items.",
        "inputSchema": {
          "title": "mssql_get_azure_devops_info",
          "description": "Gets Azure DevOps information including projects, repositories, builds, and work items.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          }
        }
      },
      {
        "name": "mssql_list_user_keys",
        "description": "List API keys for a user",
        "inputSchema": {
          "title": "mssql_list_user_keys",
          "description": "List API keys for a user",
          "type": "object",
          "properties": { "userId": { "type": "string" } },
          "required": ["userId"]
        }
      },
      {
        "name": "mssql_list_all_keys",
        "description": "List all API keys (admin only)",
        "inputSchema": {
          "title": "mssql_list_all_keys",
          "description": "List all API keys (admin only)",
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "mssql_execute_query",
        "description": "Executes a SQL query and returns the results as JSON.",
        "inputSchema": {
          "title": "mssql_execute_query",
          "description": "Executes a SQL query and returns the results as JSON.",
          "type": "object",
          "properties": {
            "query": { "type": "string" },
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          },
          "required": ["query"]
        }
      },
      {
        "name": "mssql_get_agent_jobs",
        "description": "Gets SQL Server Agent job metadata (jobs, status, owner, etc.) from msdb.",
        "inputSchema": {
          "title": "mssql_get_agent_jobs",
          "description": "Gets SQL Server Agent job metadata (jobs, status, owner, etc.) from msdb.",
          "type": "object",
          "properties": {
            "connectionName": {
              "type": "string",
              "default": "DefaultConnection"
            }
          }
        }
      },
      {
        "name": "mssql_delete_key",
        "description": "Delete an API key",
        "inputSchema": {
          "title": "mssql_delete_key",
          "description": "Delete an API key",
          "type": "object",
          "properties": { "id": { "type": "string" } },
          "required": ["id"]
        }
      }
    ]
  },
  "id": 1,
  "jsonrpc": "2.0"
}
